<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!--
            cacheEnabled: 전역 2차 캐시 활성화 (기본값 true)
            이 설정이 false 이면 <cache/> 선언이 있어도 2차 캐시가 동작하지 않습니다.
        -->
        <setting name="cacheEnabled" value="true"/>
    </settings>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver"   value="com.mysql.cj.jdbc.Driver"/>
                <property name="url"      value="jdbc:mysql://localhost:3306/sakila"/>
                <property name="username" value="simple-user"/>
                <property name="password" value="q1w2e3r4!"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="Cache/mapper/FilmCacheMapper.xml"/>
    </mappers>
</configuration>
