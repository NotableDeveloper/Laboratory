<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <!-- SQL 실행 로그를 표준 출력으로 확인 -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <!-- snake_case 컬럼명을 camelCase 프로퍼티에 자동 매핑 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver"   value="com.mysql.cj.jdbc.Driver"/>
                <property name="url"      value="jdbc:mysql://localhost:3306/sakila"/>
                <property name="username" value="simple-user"/>
                <property name="password" value="q1w2e3r4!"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <!-- XML 매퍼 파일 등록: FilmMapper.xml 의 namespace="SqlMapping.mapper.FilmMapper" 와 자동 연결 -->
        <mapper resource="SqlMapping/mapper/FilmMapper.xml"/>
    </mappers>
</configuration>
